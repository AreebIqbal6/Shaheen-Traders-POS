name: Release
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    env:
      TAURI_SIGNING_PRIVATE_KEY: |-
        untrusted comment: rsign encrypted secret key
        RWRTY0IyWClnVcKPKPQfRAOhFoRibo1jPbRaNajNFaq5WPvNKpkAABAAAAAAAAAAAAIAAAAAAL0nVUQgcmT47PlYPFS5jpmUtGOLHxTSVsH5BPsP9ga/EqqbTMNSluBeiCU7ex4j1AkycYc1Fj9GMwoIUkChurHtngHsYMMYv/mz1DCPqzFiPwjWUrMiAHVETSXNgr60Wvfz8A2dUVPaa0=
      TAURI_SIGNING_PRIVATE_KEY_PASSWORD: "ShaheenSecureKey2026!"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Build and Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Shaheen POS ${{ github.ref_name }}'
          releaseBody: 'See the assets to download and install.'
          releaseDraft: false
          prerelease: false
          includeUpdaterJson: true
