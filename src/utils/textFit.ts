export const getResponsiveFontSize = (text: string | null | undefined, baseSize = 14): string => {   if (!text) return baseSize + 'px';   const len = text.length;   if (len <= 22) return baseSize + 'px';   if (len <= 35) return Math.max(11, baseSize - 1) + 'px';   if (len <= 48) return Math.max(10, baseSize - 2) + 'px';   if (len <= 65) return Math.max(9, baseSize - 3) + 'px';   return Math.max(8.5, baseSize - 4) + 'px'; };